language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
cache: cargo

addons:
  apt:
    packages:
      - libgpg-error
      - libgpg-error-dev
      - libgpgme11-dev
